# 工程架构

## 一、整体架构

```
┌─────────────────────────────────────────────────┐
│                  index.html                       │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │  用户摘要  │  │  比赛表格  │  │  ECharts 图表  │  │
│  │    卡片    │  │  (时间线) │  │  (直方图等)   │  │
│  └──────────┘  └──────────┘  └───────────────┘  │
│          ▲           ▲              ▲            │
│          └───────────┴──────────────┘            │
│                    读取 data.js                   │
└─────────────────────────────────────────────────┘
                       ▲
                       │ 生成
┌─────────────────────────────────────────────────┐
│              cf_crawler.exe (C 程序)              │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐ │
│  │ libcurl│ │ cJSON  │ │ 数据分析│ │ HTML 生成 │ │
│  │ HTTP   │ │ JSON   │ │  统计   │ │  模块     │ │
│  │ 请求   │ │ 解析   │ │  模块   │ │           │ │
│  └────────┘ └────────┘ └────────┘ └──────────┘ │
└─────────────────────────────────────────────────┘
                       ▲
                       │ HTTP API
┌─────────────────────────────────────────────────┐
│              Codeforces API                      │
│         https://codeforces.com/api/              │
└─────────────────────────────────────────────────┘
```

## 二、运行流程

1. 用户运行 `cf_crawler.exe <user_id>` 或读取 `users.txt` 多用户列表
2. C 程序通过 libcurl 调用 Codeforces API 获取数据
3. cJSON 解析返回的 JSON 数据
4. 数据分析模块统计计算
5. 输出：将全部数据写入 `data.js`（JS 变量格式）+ 生成带内嵌数据的 `index.html`
6. 用户在浏览器打开 `index.html` 即可看到可视化结果

## 三、目录结构

```
D:\codeforce\
├── src/                     # C 源代码
│   ├── main.c               # 入口，命令行参数处理
│   ├── http_client.c/.h     # libcurl 封装
│   ├── cf_api.c/.h          # Codeforces API 调用
│   ├── data_model.c/.h      # 数据结构定义与管理
│   ├── analyzer.c/.h        # 数据分析与统计
│   ├── html_gen.c/.h        # HTML/JS 页面生成
│   └── utils.c/.h           # 工具函数（颜色映射等）
├── lib/                     # 第三方库
│   ├── curl/                # libcurl 头文件与库
│   └── cJSON/               # cJSON 源码
├── web/                     # 前端资源
│   ├── index.html           # 前端页面模板
│   ├── style.css            # 样式表
│   └── echarts.min.js       # ECharts 库（CDN 或本地）
├── output/                  # 程序输出目录
│   ├── index.html           # 生成的结果页面
│   └── data.js              # 生成的数据文件
├── users.txt                # 多用户 ID 列表（选作）
├── Makefile                 # 编译脚本
└── 架构.md                  # 本文件
```

## 四、Codeforces API 接口

| 接口 | 用途 | URL |
|------|------|-----|
| user.info | 用户基本信息（等级分、头衔、头像） | `/api/user.info?handles={handle}` |
| user.rating | 用户 rating 变化历史 | `/api/user.rating?handle={handle}` |
| user.status | 用户提交记录 | `/api/user.status?handle={handle}` |
| contest.list | 比赛列表 | `/api/contest.list` |

## 五、数据结构

```c
// 用户基本信息
typedef struct {
    char handle[64];
    int rating;
    int maxRating;
    char rank[32];        // 头衔
    char avatar[256];     // 头像URL
    int contribution;
    char rankColor[16];   // 等级分对应颜色
} UserInfo;

// 单场比赛信息
typedef struct {
    int contestId;
    char contestName[256];
    long long startTime;      // Unix 时间戳
    int oldRating;
    int newRating;
    int rank;                 // 比赛排名
    int points;               // 得分
    int problemCount;         // 题目数
    int solvedCount;          // 通过题数
    char problemResults[16][32]; // 各题结果
    int upsolvedCount;        // 赛后补题数
    char upsolvedProblems[16][8]; // 补题题号
} ContestRecord;

// 用户完整数据
typedef struct {
    UserInfo info;
    int contestCount;         // 总比赛次数
    int recentContestCount;   // 近180天比赛次数
    int recentMaxRating;      // 近180天最高等级分
    ContestRecord *contests;  // 比赛记录列表
    int contestNum;           // 比赛数量
} UserData;
```

## 六、CF 等级分颜色映射

| 等级分范围 | 颜色 | 色值 | 头衔 |
|-----------|------|------|------|
| < 1200 | 灰 | #808080 | Newbie |
| 1200-1399 | 绿 | #008000 | Pupil |
| 1400-1599 | 青 | #03A89E | Specialist |
| 1600-1899 | 蓝 | #0000FF | Expert |
| 1900-2099 | 紫 | #AA00AA | Candidate Master |
| 2100-2299 | 橙 | #FF8C00 | Master |
| 2300-2399 | 橙 | #FF8C00 | International Master |
| 2400-2599 | 红 | #FF0000 | Grandmaster |
| 2600-2999 | 红 | #FF0000 | International Grandmaster |
| ≥ 3000 | 红/黑 | #FF0000 | Legendary Grandmaster |

## 七、开发顺序（计划）

### 阶段一：基础设施搭建
- [x] 1.1 搭建目录结构，配置 Makefile
- [x] 1.2 集成 cJSON 源码
- [x] 1.3 封装 libcurl HTTP 请求模块（http_client.c）
- [x] 1.4 定义数据结构（data_model.h）

### 阶段二：API 与数据处理
- [x] 2.1 实现 Codeforces API 调用（cf_api.c）
  - user.info 接口
  - user.rating 接口
  - user.status 接口
  - contest.list 接口
- [x] 2.2 实现数据分析模块（analyzer.c）
  - 统计比赛次数、最高等级分
  - 近180天筛选
  - 赛后补题匹配
  - 题目难度等级分统计

### 阶段三：前端可视化
- [x] 3.1 HTML 页面结构 + CSS 样式
- [x] 3.2 用户信息卡片（头像、等级分、头衔、颜色）
- [x] 3.3 比赛历史表格（时间线，按时间倒序）
- [x] 3.4 ECharts 等级分变化折线图

### 阶段四：选作功能
- [x] 4.1 多用户列表支持（读取 users.txt + 摘要表格 + 页面切换）
- [x] 4.2 难度直方图（全部/近一年/近180天/近1月）

### 阶段五：收尾
- [x] 5.1 编译测试，生成可执行程序
- [x] 5.2 编写设计文档
- [x] 5.3 最终调试与优化

**✅ 全部完成（2026-05-23）**
