# Codeforces Rating Crawler — 使用文档

## 一、项目简介

`cf_crawler` 是一个命令行工具，调用 [Codeforces API](https://codeforces.com/apiHelp) 获取用户数据，生成包含 Rating 曲线、难度直方图、比赛详情的交互式 HTML 页面。

**核心能力**：输入 CF 用户名 → 自动拉取数据 → 生成可视化页面。

---

## 二、运行环境

| 需求 | 说明 |
|------|------|
| 操作系统 | Windows 64-bit |
| 运行时依赖 | `libcurl-x64.dll`（与 exe 同目录即可） |
| 网络 | 可访问 `https://codeforces.com/api/` |

---

## 三、快速开始

### 1. 准备文件

确保以下文件在同一目录下：

```
cf_crawler.exe
libcurl-x64.dll
web\template.html         （或在 exe 同目录放一份 template.html）
```

### 2. 运行（单用户）

在 PowerShell 或 CMD 中：

```powershell
.\cf_crawler.exe tourist
```

生成文件（位于 `output\` 子目录）：
- `output\data.js` — 用户数据（JavaScript 变量格式）
- `output\index.html` — 可视化页面（可直接用浏览器打开）

### 3. 查看结果

在浏览器中打开 `output\index.html`，即可看到：
- 用户信息卡片（头像、等级分、头衔）
- Rating 变化曲线（ECharts 折线图，节点按 CF 等级分色着色）
- 难度直方图（4 时段 tab 切换）
- 比赛详情表格（排名、分差、每题通过状态、补题列表）

---

## 四、四种运行模式

### 模式一：交互模式（双击启动）

**直接双击 `cf_crawler.exe`**（或在命令行不带任何参数运行）：

```powershell
.\cf_crawler.exe
```

程序将进入交互模式，提示输入 CF 用户名：

```
Enter Codeforces handle: tourist
```

自动拉取数据后，生成：
- `output\{handle}_data.js` — 用户数据文件
- `output\{handle}.html` — 个人可视化页面

查看完毕后按 Enter 退出。

---

### 模式二：单用户（命令行）

```powershell
.\cf_crawler.exe <handle>
```

示例：
```powershell
.\cf_crawler.exe tourist
```

输出：
- `output\data.js`
- `output\index.html`

---

### 模式三：多用户（命令行参数）

```powershell
.\cf_crawler.exe <handle1> <handle2> <handle3> ...
```

示例：
```powershell
.\cf_crawler.exe tourist Petr Benq
```

输出（均在 `output\` 目录下）：
| 文件 | 说明 |
|------|------|
| `tourist_data.js` + `tourist.html` | tourist 个人页 |
| `Petr_data.js` + `Petr.html` | Petr 个人页 |
| `Benq_data.js` + `Benq.html` | Benq 个人页 |
| `index.html` | 多用户列表页（排序 + 跳转） |

---

### 模式四：从文件批量读取

```powershell
.\cf_crawler.exe <文件名.txt>
```

文件格式（每行一个 handle，支持 `#` 注释和空行）：

```
# 我的关注列表
tourist
Petr
Benq
Gennady
```

示例：
```powershell
.\cf_crawler.exe users.txt
```

输出与「模式三」相同。

---

## 五、输出的 HTML 页面说明

### 个人页面（如 `tourist.html`）

| 区域 | 内容 |
|------|------|
| 顶部卡片 | 头像、昵称（CF 等级分色）、当前分 / 最高分 / 头衔 |
| 统计行 | 当前分、最高分、总场次、近 180 天场次 / 最高分 |
| Rating 曲线 | ECharts 折线图，节点按对应等级分色着色，最高/最低分 ⭐ 标记 |
| 难度直方图 | 4 时段 tab（全部 / 365天 / 180天 / 30天），X 轴难度区间，Y 轴通过题数 |
| 比赛表格 | 日期、赛事名、排名、分差、各题状态（绿=通过 红=失败 灰=未做）、补题列表 |

### 列表页（多用户模式 `index.html`）

| 功能 | 说明 |
|------|------|
| 统计卡片 | 用户数、总场次、全场最高 Max Rating |
| 用户表格 | 头像、昵称、Rating、Max、Rank、Contests、180天场次/最高分 |
| 排序 | 点击表头按列排序 |
| 跳转 | 点击行跳转对应用户的详情页 |

---

## 六、从源码编译

### 环境要求

| 工具 | 版本 |
|------|------|
| MinGW-w64 GCC | 8.1.0+ |
| GNU Make | 3.81+ |
| libcurl 开发包 | 8.x（头文件 + 静态库） |

### 编译步骤

```powershell
# 1. 进入项目根目录
cd D:\codeforce

# 2. 编译
mingw32-make all

# 3. 运行测试
mingw32-make test

# 4. 打包分发
mingw32-make dist
```

### Makefile 目标

| 目标 | 说明 |
|------|------|
| `all` | 编译 `cf_crawler.exe` |
| `test` | 用 `tourist` 运行一次测试 |
| `clean` | 清理编译产物和生成文件 |
| `dist` | 打包 exe + dll + template → `dist/` |

---

## 七、常见问题

**Q: 双击 exe 一闪而过？**
A: 直接双击 exe 会自动进入交互模式，提示输入用户名。如果仍然闪退，请在 PowerShell / CMD 中运行以查看错误信息。

**Q: 提示 "libcurl-x64.dll 缺失"？**
A: 将 `libcurl-x64.dll` 放入 `cf_crawler.exe` 所在目录。

**Q: 网络超时或请求失败？**
A: Codeforces API 有频率限制。程序已内置 1.5 秒请求间隔。单次超时设为 30s，如失败可稍等重试。建议每次拉取不要超过 5 个用户。

**Q: 生成的 HTML 打不开 / 图表不显示？**
A: 图表依赖 CDN 加载 ECharts，需要网络连接。确保浏览器能访问 `cdn.jsdelivr.net`。

**Q: 某个 handle 拉取失败？**
A: 检查 handle 拼写是否正确（区分大小写），或网络是否正常。多用户模式下单个失败不影响其他用户。

---

## 八、限制说明

- `user.status` 采用分页自动拉取，单次 1000 条，最多 10 页共 **10,000 条**提交（CF API 硬上限）
- 对于提交量极大的用户（＞10,000 题），仍可能丢失极早期数据；绝大多数用户不受影响
- 所有数据来源于 Codeforces API，生成 HTML 时不具备实时更新能力——如需更新需重新运行
- 偶尔遇到 CF API 503 时需稍等后重试

---

## 九、项目文件结构

```
D:\codeforce\
├── cf_crawler.exe           # 可执行程序（编译产物）
├── libcurl-x64.dll          # 运行时依赖（需随 exe 分发）
├── Makefile                 # 编译脚本（GNU Make）
├── users.txt                # 用户列表示例
│
├── src\                     # C 源代码
│   ├── main.c               # 程序入口（四种模式调度）
│   ├── http_client.c/.h     # libcurl HTTP 封装
│   ├── cf_api.c/.h          # Codeforces API 调用
│   ├── analyzer.c/.h        # 数据分析 + JS 导出
│   ├── data_model.c/.h      # 数据结构定义
│   └── utils.c/.h           # 工具函数（颜色/等级映射）
│
├── lib\                     # 第三方库
│   ├── cJSON\               # cJSON 源码 (v1.7.18)
│   └── curl\                # libcurl 开发包
│
├── web\                     # 前端资源
│   └── template.html        # HTML 页面模板（暗色主题 + ECharts）
│
├── output\                  # 输出目录（程序运行产物）
│   ├── data.js              # 单用户模式数据
│   ├── index.html           # 单用户 / 多用户列表页
│   ├── {handle}_data.js     # 多用户模式数据
│   └── {handle}.html        # 多用户模式个人页
│
├── Script说明\              # 各模块详细说明文档
├── 架构.md                  # 工程架构设计
├── 设计文档.md              # 详细设计文档
├── 使用文档.md              # 本文件
└── log.md                   # 开发日志
```

---

> 最后更新：2026-05-30
