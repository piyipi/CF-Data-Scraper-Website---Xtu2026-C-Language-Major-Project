# 对话进度 —— 逐函数代码注释任务

## 目标

按 `项目概述.md` 的架构分层（自底向上：第1层 → 第8层），逐个函数解释项目代码并写入注释。每个函数需用户说"下一个"才继续，或先解释再等用户说"写入"。

---

## 总体进度

| 层 | 模块 | 状态 | 函数数 | 完成 |
|----|------|------|--------|------|
| 1 | cJSON（第三方） | ⏭ 跳过 | - | - |
| 2 | http_client | ✅ 完成 | 4 | 4/4 |
| 3 | data_model | ✅ 完成 | 1 | 1/1 |
| 4 | utils | ✅ 完成 | 2 | 2/2 |
| 5 | cf_api | ✅ 完成 | 4 | 4/4 |
| 6 | analyzer | ✅ 完成 | 3 | 3/3 |
| 7 | main.c | 🔄 进行中 | 6 | 2/6 |
| 8 | template.html | ⬜ 待开始 | - | - |

---

## 已完成详情

### 第7层：main.c（`src/main.c`）🔄 进行中

| # | 函数 | 状态 | 说明 |
|---|------|------|------|
| 1 | `interactive_mode` (static) | ✅ | 交互模式入口，三种输入格式自动识别（单/多/文件），strtok 分词 + 动态扩容 |
| 2 | `process_handles` (static) | ✅ | 共享处理逻辑——由 main 和 interactive_mode 共用，calloc + 循环 analyze_user + 单/多用户分支输出 |
| 3 | `str_ends_with` (static) | ✅ | 后缀匹配，判断 .txt 文件模式 |
| 4 | `read_handles_file` (static) | ⬜ | 读取用户列表文件，跳过空行和 # 注释 |
| 5 | `generate_list_page` (static) | ⬜ | 多用户列表页，fprintf 硬编码 HTML + 内嵌排序 JS |
| 6 | `main` | ⬜ | 入口：交互/单/多/文件 四种模式判断 → http_init → process_handles → http_cleanup |

**额外记录**：
- `process_single` 函数已在源码中移除（main.md 提到但实际不存在）

---

### 第2层：http_client（`src/http_client.c` + `src/http_client.h`）

| # | 函数 | 说明 |
|---|------|------|
| 1 | `write_callback` | libcurl 写回调，逐块 realloc 拼接响应体，末尾补 \0 |
| 2 | `http_init` | 全局初始化 curl_global_init，幂等保护（g_initialized 标志） |
| 3 | `http_cleanup` | 全局清理 curl_global_cleanup，幂等保护 |
| 4 | `http_get` | HTTP GET 请求，最多 3 次重试，429 限流等 2s，其余错误等 1s |

**额外记录**：
- libcurl Global API 说明（curl_global_init / curl_global_cleanup）
- libcurl Easy API 说明（init / setopt / perform / getinfo / cleanup / strerror）
- 设计模式「Init/Cleanup 配对 + 幂等保护」已写入 `项目设计模式.md`

### 第5层：cf_api（`src/cf_api.h` + `src/cf_api.c`）

| # | 函数 | 说明 |
|---|------|------|
| 1 | `api_get` (static) | CF API 统一请求管线：URL 组装 → HTTP GET → JSON 解析 → status=="OK" 校验，四步流水线 |
| 2 | `cf_get_user_info` | 调用 user.info API，提取 handle/rating/maxRating/rank/avatar，填充 UserInfo 结构体 |
| 3 | `cf_get_user_ratings` | 调用 user.rating API，一次 malloc 分配 ContestRecord 数组，逐条 memset 清零后填充 |
| 4 | `cf_get_user_status` | 调用 user.status API，分页拉取（1000条/页，最多10000条），Detach+AddItem 合并为单一 cJSON 树 |

**额外记录**：
- 注释格式统一为：一、调用的 Codeforces API / 二、cJSON 库函数 / 三、设计要点
- `cf_api.md` 同步修正：移除不存在的 `cf_get_contest_list`、更新 `cf_get_user_status` 分页描述（count:200→1000）

---

### 第6层：analyzer（`src/analyzer.h` + `src/analyzer.c`）

| # | 函数 | 说明 |
|---|------|------|
| 1 | `analyze_user` | 6 阶段聚合：拉取 API → ProbState 补题匹配 → 难度直方图，失败降级不中断 |
| 2 | `escape_json_str` (static) | JS 字符串 5 种转义（\ → \\、 " → \" 等），防溢出 |
| 3 | `export_data_js` | fprintf 直接拼接 `var CF_DATA = {...};`，比赛倒序输出 |

**额外记录**：
- `analyzer.md` 同步修正：移除不存在的 `analyze_multiple`、更新行数、更新 user.status 分页描述

---

### 第3层：data_model（`src/data_model.h` + `src/data_model.c`）

| # | 函数 | 说明 |
|---|------|------|
| 1 | `userdata_free` | 释放 UserData.records 动态数组，防 double-free |

**上下文结构体**（仅定义，无函数）：
- `ProblemResult` — 单题比赛结果
- `UserInfo` — CF 用户基本信息
- `ContestRecord` — 单场比赛记录（最多 12 题 + 补题列表）
- `UserData` — 用户聚合数据（含 4 时段难度直方图）

### 第4层：utils（`src/utils.c` + `src/utils.h`）

| # | 函数 | 说明 |
|---|------|------|
| 1 | `cf_color` | CF 等级分 → 十六进制颜色，8 级阶梯映射（灰→暗红） |
| 2 | `days_since` | Unix 时间戳距今多少天，difftime 安全计算，86400 取整 |

**额外记录**：
- `difftime` 函数详细说明（与直接相减对比，可移植性）

---

## 产出文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 注释写入 | `src/http_client.c` | 4 个函数全部已注释 |
| 注释写入 | `src/data_model.c` | userdata_free 已注释 |
| 注释写入 | `src/utils.c` | cf_color、days_since 已注释 |
| 注释写入 | `src/cf_api.c` | 4 个函数全部已注释（api_get 含 cJSON 库函数 + 设计要点） |
| 注释写入 | `src/analyzer.c` | 3 个函数全部已注释 |
| 注释写入 | `src/main.c` | interactive_mode、process_handles、str_ends_with 已注释（3/6，进行中） |
| 设计模式 | `Script说明/项目设计模式.md` | 已记录 Init/Cleanup 模式 |

---

## 下一步

**第7层：`main.c`** — 继续剩余函数：`read_handles_file` → `generate_list_page` → `main`

---

## 工作约定

1. **默认**：先给出函数解释（作用、核心逻辑、设计要点），等用户确认
2. **"写入"**：将解释转为注释写入 .c 文件
3. **"下一个"**：跳过当前函数注释，进入下一个函数
4. 发现设计模式 → 追加到 `Script说明/项目设计模式.md`
5. 层完成 → 更新本进度文档
