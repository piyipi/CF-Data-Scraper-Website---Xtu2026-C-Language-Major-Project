# Codeforces Rating Crawler — 设计文档

## 一、项目概述

利用 Codeforces API 抓取用户信息，对其比赛情况进行多维度统计分析，并通过 ECharts 实现可视化展示。

**技术栈**：C 语言 + libcurl + cJSON + ECharts（前端）

**开发环境**：MinGW-w64 (GCC 8.1.0) / Windows 10+

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│              index.html（前端可视化）                  │
│  ┌──────────┐  ┌──────────┐  ┌─────────────────┐    │
│  │ 用户卡片  │  │ 比赛表格  │  │  ECharts 图表    │    │
│  │(头像/分) │  │ (时间线) │  │ (折线+直方图)  │    │
│  └──────────┘  └──────────┘  └─────────────────┘    │
│          ▲           ▲              ▲                │
│          └───────────┴──────────────┘                │
│                   ← data.js ←                       │
└─────────────────────────────────────────────────────┘
                      ▲
                      │ 生成
┌─────────────────────────────────────────────────────┐
│           cf_crawler.exe（C 程序）                    │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐    │
│  │libcurl │ │ cJSON  │ │数据分析│ │ HTML 生成 │    │
│  │HTTP GET│ │JSON解析│ │  统计  │ │  JS 导出  │    │
│  └────────┘ └────────┘ └────────┘ └──────────┘    │
└─────────────────────────────────────────────────────┘
                      ▲
                      │ HTTPS
┌─────────────────────────────────────────────────────┐
│            Codeforces API                            │
│        https://codeforces.com/api/                   │
└─────────────────────────────────────────────────────┘
```

---

## 三、模块设计

### 3.1 模块总览

| 文件 | 作用 | 行数 |
|------|------|------|
| `main.c` | 程序入口，命令行解析，单/多/文件三种模式 | 381 |
| `http_client.h/c` | libcurl 封装，HTTP GET 请求 | 91 |
| `cf_api.h/c` | Codeforces API 四接口调用 | ~180 |
| `analyzer.h/c` | 数据分析、补题匹配、统计聚合、JS 导出 | 302 |
| `data_model.h/c` | 数据结构定义与内存管理 | ~60 |
| `utils.h/c` | CF 等级分颜色/头衔映射，日期工具 | ~70 |
| `cJSON.h/c` | 第三方 JSON 解析库 (v1.7.18) | ~2800 |
| `template.html` | 前端模板（暗色主题 + ECharts） | 697 |

### 3.2 http_client 模块

**职责**：封装 libcurl，提供统一的 HTTP GET 接口。

| 函数 | 功能 |
|------|------|
| `http_init()` | curl_global_init 全局初始化 |
| `http_cleanup()` | curl_global_cleanup 全局清理 |
| `http_get(url, response)` | GET 请求，30s 超时，自动跟随重定向，仅 200 返回成功 |

**关键设计**：

- write callback 使用 `realloc` 动态累积响应数据，末尾自动补 `\0`
- User-Agent: `CF-Crawler/1.0`
- 响应体由调用者负责 `free(response.data)`

### 3.3 cf_api 模块

**职责**：调用 4 个 Codeforces API 接口。

| 函数 | API | 返回类型 |
|------|-----|---------|
| `cf_get_user_info(handle, info)` | `user.info` | UserInfo 结构体 |
| `cf_get_user_ratings(handle, records, count)` | `user.rating` | ContestRecord[] 动态数组 |
| `cf_get_user_status(handle, root)` | `user.status` | cJSON 原始对象 |
| `cf_get_contest_list(root)` | `contest.list` | cJSON 原始对象 |

**内部实现**：统一调用 `api_get(url, root)` → URL 组装 → HTTP GET → JSON 解析 → status==OK 校验。

**API 端点**：

| 接口 | URL | 参数 |
|------|-----|------|
| user.info | `/api/user.info` | `?handles={handle}` |
| user.rating | `/api/user.rating` | `?handle={handle}` |
| user.status | `/api/user.status` | `?handle={handle}&from=1&count=1000`（自动分页，最多 10000 条） |
| contest.list | `/api/contest.list` | `?gym=false` |

### 3.4 analyzer 模块

**职责**：数据聚合、统计分析、JS 文件导出。

| 函数 | 功能 |
|------|------|
| `analyze_user(handle, ud)` | 拉取信息 + 比赛历史 + 提交记录，三步聚合填充 UserData |
| `export_data_js(ud, path)` | 将 UserData 导出为 `var CF_DATA = {...};` 格式 JS 文件 |
| `analyze_multiple(handles, count, out)` | 批量分析多用户（选作） |

**核心分析逻辑**：

1. **基本信息**：从 `user.info` 填充 UserInfo
2. **比赛历史**：遍历 `user.rating`，填充 ContestRecord 数组，按时间从近到远排序
3. **补题匹配**：遍历 `user.status` 提交记录，以 `ratingUpdateTimeSeconds` 为赛时/赛后分界线
   - 赛时提交：记录各题得分（有 `points` 取 `points`，无 `points` 按 OK 给 1.0）
   - 赛后提交：OK 的记录计入补题列表（upsolved）
4. **统计指标**：
   - `contestCount`：总比赛场次
   - `recentCount180d`：近 180 天比赛场次
   - `recentMaxRating180d`：近 180 天最高等级分
   - `histogram[4][28]`：4 个时段（全部/365d/180d/30d）× 28 个难度桶的通过题数

### 3.5 main 入口

**三种运行模式**：

| 命令 | 模式 | 行为 |
|------|------|------|
| `cf_crawler tourist` | 单用户 | 生成 `data.js` + `index.html` |
| `cf_crawler user1 user2` | 多用户 | 各自 `{handle}_data.js` + `{handle}.html` + 列表页 `index.html` |
| `cf_crawler users.txt` | 文件 | 读取文件 handles（`#` 注释支持），行为同多用户 |

---

## 四、数据结构

项目共涉及 6 个核心自定义结构体和 3 个内部/辅助结构体，定义在 4 个源文件中。所有结构体按数据流向可分为三层：**传输层**（HTTP 响应）、**模型层**（比赛数据）、**聚合层**（用户全量数据）。

---

### 4.1 结构体总览

| 结构体 | 文件:行号 | 层级 | 内存策略 | 用途 |
|--------|----------|------|----------|------|
| `HttpResponse` | `http_client.h:7-10` | 传输层 | 动态分配 `.data`，调用者 free | HTTP 响应体包装 |
| `ProblemResult` | `data_model.h:7-12` | 模型层 | 内联数组（值语义，不单独分配） | 单题比赛结果 |
| `UserInfo` | `data_model.h:15-22` | 模型层 | 值成员（嵌入 UserData） | 用户基本画像 |
| `ContestRecord` | `data_model.h:27-39` | 模型层 | 动态数组（malloc 整块分配） | 单场比赛完整记录 |
| `UserData` | `data_model.h:48-56` | 聚合层 | calloc 数组 + 内含动态 records | 用户全量聚合数据 |
| `WriteCtx` | `http_client.c:12-15` | 传输层 | 栈变量（内部，不公开） | libcurl 写回调累积缓冲 |
| `ProbState` | `analyzer.c:61-67` | 聚合层 | 栈数组（局部临时变量） | 补题匹配中间态 |

---

### 4.2 模型层结构体详解

#### 4.2.1 ProblemResult — 单题比赛结果

- **位置**：`src/data_model.h` 第 7-12 行
- **职责**：记录用户在某场比赛的某道题上的表现。
- **字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `index[4]` | `char[]` | 题号，如 `"A"`、`"B1"`、`"C"` |
| `points` | `double` | 该题得分（CF 赛制下每题分数，非 IOI 则 0 或满分） |
| `attempt_count` | `int` | 提交次数（含失败尝试） |
| `best_time` | `int` | 首次通过时间（秒，距比赛开始）。未通过则为 -1 |

- **内存策略**：以固定数组 `ProblemResult problems[12]` 的形式嵌入 `ContestRecord`，不单独分配。
- **数据来源**：`analyzer.c` 第 136-140 行，从临时 `struct ProbState` 填充。
- **数据消费**：`export_data_js()` 第 248-250 行，序列化为 JS 对象。

---

#### 4.2.2 UserInfo — 用户基本信息

- **位置**：`src/data_model.h` 第 15-22 行
- **职责**：存储从 `user.info` API 获取的用户画像数据。
- **字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `handle[64]` | `char[]` | CF 用户名 |
| `rating` | `int` | 当前等级分 |
| `max_rating` | `int` | 历史最高等级分 |
| `rank_name[32]` | `char[]` | 头衔，如 `"Expert"`、`"Grandmaster"` |
| `avatar_url[256]` | `char[]` | 头像图片 URL |
| `cf_color[16]` | `char[]` | 当前等级分对应的十六进制颜色值，由 `cf_color()` 函数计算填充 |

- **内存策略**：以值成员 `UserInfo info` 形式嵌入 `UserData`，不单独分配。
- **生产者**：`cf_get_user_info()`（`cf_api.c:52-101`），从 JSON 逐字段解析填充。
- **消费者**：前端 HTML 生成（`main.c:307-319` 读取 handle/avatar/color/rating）、`export_data_js()` 序列化。

> **关于旧架构文档中的差异**：旧 `架构.md` 中的 `UserInfo` 定义（含 `contribution` 字段、`maxRating` 驼峰命名等）与实际代码（`data_model.h`）不一致。本文档以实际代码为准。

---

#### 4.2.3 ContestRecord — 单场比赛记录

- **位置**：`src/data_model.h` 第 27-39 行
- **职责**：存储用户在某场 CF 比赛中的完整战绩，是数据分析的核心数据单元。
- **字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `contest_id` | `int` | CF 比赛唯一 ID |
| `contest_name[256]` | `char[]` | 比赛名称 |
| `start_time` | `time_t` | 比赛开始时间（Unix 时间戳） |
| `old_rating` | `int` | 赛前等级分 |
| `new_rating` | `int` | 赛后等级分 |
| `rank` | `int` | 比赛排名 |
| `points` | `double` | 总得分 |
| `problem_count` | `int` | 本场比赛题目数量 |
| `problems[12]` | `ProblemResult[]` | 各题详情（`MAX_PROBLEMS = 12`） |
| `upsolved_count` | `int` | 赛后补题数量 |
| `upsolved[12][4]` | `char[][]` | 补做题号数组，如 `"A"`、`"B2"` |

- **内存策略**：以**动态数组**形式存储（`ContestRecord *records`），由 `cf_get_user_ratings()` 通过 `malloc(n * sizeof(ContestRecord))` 一次性分配整块内存（`cf_api.c:129`）。
- **生产者**：`cf_get_user_ratings()`（`cf_api.c:103-164`），解析 `user.rating` API 的 JSON 数组，逐条填充每个 `ContestRecord`。
- **写入者**：`analyze_user()` 主循环（`analyzer.c:45-185`），为每条记录填充 `problems[]` 和 `upsolved[]`。
- **消费者**：`export_data_js()` 逆序遍历输出 JS（`analyzer.c:237-270`）；`generate_list_page()` 统计总场次。
- **释放者**：`userdata_free()` → `free(ud->records)`（`data_model.c:7`）。

> **关于 `upsolved` 类型差异**：旧 `架构.md` 中 `upsolved` 为 `int` 类型（存储题号索引），实际代码已改为 `char[12][4]` 字符串数组（直接存题号字符串如 `"B1"`），以匹配前端渲染需求。

---

#### 4.2.4 UserData — 用户聚合数据（顶层结构体）

- **位置**：`src/data_model.h` 第 48-56 行
- **职责**：聚合单个用户的**全部数据**——基本信息、比赛历史、统计指标、难度直方图。是程序内部数据模型的**根节点**。
- **字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `info` | `UserInfo` | 用户基本信息（值嵌入） |
| `contest_count` | `int` | 历史总比赛场次 |
| `recent_count_180d` | `int` | 近 180 天比赛场次 |
| `recent_max_rating_180d` | `int` | 近 180 天最高等级分 |
| `records` | `ContestRecord*` | 比赛记录动态数组（指针） |
| `record_count` | `int` | 记录数量 |
| `histogram[4][28]` | `int[][]` | 难度直方图矩阵 |

- **直方图维度说明**：
  - 第 1 维（4 个时段）：全部时间 / 近 365 天 / 近 180 天 / 近 30 天
  - 第 2 维（28 个难度桶）：800–3499 等级分区间，步长 100（宏 `HISTOGRAM_BUCKETS=28`、`HISTOGRAM_MIN=800`、`HISTOGRAM_STEP=100`）
  - 值含义：该时段内该难度桶的**通过题目总数**
- **内存策略**：在 `main.c:93` 通过 `calloc(hcount, sizeof(UserData))` 分配数组（支持多用户批量），内部 `records` 指针由 `cf_get_user_ratings()` 通过 `malloc` 单独分配。
- **生产者**：`analyze_user()`（`analyzer.c:12-188`），依次调用 3 个 cf_api 接口 + 补题匹配 + 直方图统计。
- **消费者**：`export_data_js()` 导出 JS、`generate_list_page()` 生成 HTML 列表页。
- **释放**：`userdata_free()` 释放内部 `records`（`data_model.c:4-9`），随后 `main.c:170` 释放 `UserData` 数组本身。

---

### 4.3 传输层结构体

#### 4.3.1 HttpResponse — HTTP 响应体

- **位置**：`src/http_client.h` 第 7-10 行
- **职责**：封装 libcurl 的 HTTP GET 响应。`data` 由 `write_callback` 通过 `realloc` 动态累积，末尾自动补 `\0`。
- **字段**：`char *data`（响应体）、`size_t size`（长度，不含 `\0`）。
- **生命周期**：调用者栈分配 → `http_get()` 填充 `data` 指针 → 调用者 `free(resp.data)`。
- **使用位置**：`cf_api.c:24` 栈分配，`cf_api.c:31` 释放。

#### 4.3.2 WriteCtx — libcurl 写回调上下文

- **位置**：`src/http_client.c` 第 12-15 行（**private，未在头文件公开**）
- **职责**：libcurl `CURLOPT_WRITEFUNCTION` 回调的参数载体，累积 HTTP 响应片段。
- **字段**：`char *data`（累积缓冲）、`size_t size`（已累积长度）。
- **生命周期**：`http_get()` 栈变量 → `write_callback()` 中 `realloc` 扩容 → 成功时 `data` 转移给 `HttpResponse`，失败时 `free`。

---

### 4.4 内部辅助结构体

#### 4.4.1 ProbState — 补题匹配中间态

- **位置**：`src/analyzer.c` 第 61-67 行（**局部结构体，仅存在于 `analyze_user` 函数内**）
- **职责**：在单场比赛的赛时提交遍历中，暂存每题的赛时成绩和赛后补题状态。
- **字段**：`char index[4]`、`int attempts`、`double points`、`int best_time`、`int upsolved`。
- **数据流**：`ProbState ps[12]` 栈数组 → 匹配完成后逐字段写入 `ContestRecord.problems[]` 和 `ContestRecord.upsolved[]`。

---

### 4.5 关键宏常量

| 宏 | 值 | 文件:行号 | 用途 |
|----|-----|-----------|------|
| `MAX_PROBLEMS` | `12` | `data_model.h:25` | 单场比赛最大题目数（CF 比赛中通常 ≤ 8 题，12 为安全上限） |
| `HISTOGRAM_PERIODS` | `4` | `data_model.h:42` | 直方图时段数量（全部/365d/180d/30d） |
| `HISTOGRAM_BUCKETS` | `28` | `data_model.h:43` | 直方图难度桶数量（800~3500，含边界） |
| `HISTOGRAM_MIN` | `800` | `data_model.h:44` | 直方图最低等级分（CF 最低为 0，800 起可覆盖所有题目） |
| `HISTOGRAM_STEP` | `100` | `data_model.h:45` | 直方图等级分步长（每个桶覆盖 100 分范围） |
| `CF_API_BASE` | `"https://codeforces.com/api"` | `cf_api.h:7` | Codeforces API 基础 URL |
| `CF_STATUS_PAGE_SIZE` | `1000` | `cf_api.c:171` | user.status 分页拉取每页条数（API 上限） |

---

### 4.6 数据流全景

```
CF Servers (HTTPS)
  │
  ▼ http_get(url, &resp)                          [http_client.c]
  │  内部: struct WriteCtx → realloc 累积 → HttpResponse
  │  调用者: cf_api.c:24 栈分配 HttpResponse resp
  │  调用者: cf_api.c:31 free(resp.data)
  │
  ▼ cf_get_user_info(handle, &info)               [cf_api.c:52]
  │  解析 user.info JSON → 填充 UserInfo (值语义，嵌入 UserData)
  │
  ▼ cf_get_user_ratings(handle, &records, &count) [cf_api.c:103]
  │  malloc(n * sizeof(ContestRecord)) → 填充 ContestRecord 数组
  │  所有权转移给 UserData.records
  │
  ▼ cf_get_user_status(handle, &root)             [cf_api.c:166]
  │  分页拉取提交 → 返回 cJSON 原始对象
  │  调用者: analyzer.c:43，用后 cJSON_Delete(root)
  │
  ▼ analyze_user(handle, &userdata)               [analyzer.c:12]
  │  struct ProbState ps[12] (栈临时变量)
  │  (1) 调用 cf_get_user_info → userdata.info
  │  (2) 调用 cf_get_user_ratings → userdata.records + .record_count
  │  (3) 调用 cf_get_user_status → 逐比赛匹配:
  │      赛时提交 → ps[].points/attempts/best_time
  │      赛后提交 → ps[].upsolved
  │      → 写入 rec.problems[] / rec.upsolved[]
  │  (4) 遍历 OK 提交 → 统计 histogram[4][28]
  │  (5) 计算 contest_count / recent_count_180d / recent_max_rating_180d
  │
  ▼ 输出层
  │  export_data_js(const UserData*, path) → data.js
  │  generate_list_page(UserData*, count, path) → index.html
  │
  ▼ 清理
     userdata_free(&userdata) → free(records)
     free(users) → 释放 UserData 数组
```

---

### 4.7 cJSON 库数据结构（第三方）

项目使用 cJSON v1.7.18 作为 JSON 解析库（源码位于 `lib/cJSON/`）。其核心结构体为实现细节，项目代码不直接操作其内部字段，仅通过公开 API 访问。以下为简单参考：

| 结构体 | 文件 | 用途 |
|--------|------|------|
| `cJSON` | `cJSON.h:97-117` | JSON 节点，双向链表 + 树形结构（`next/prev/child`） |
| `cJSON_Hooks` | `cJSON.h:119-124` | 自定义内存分配器钩子 |
| `internal_hooks` | `cJSON.c:156-161` | 内部内存管理函数表（malloc/free/realloc 指针） |
| `parse_buffer` | `cJSON.c:289-296` | 解析上下文（内容指针 + 偏移 + 嵌套深度） |
| `printbuffer` | `cJSON.c:478-487` | 序列化输出缓冲 |
| `error` | `cJSON.c:88-91` | 解析错误定位信息 |

---

## 五、CF 等级分颜色映射

| 范围 | 颜色 | 色值 | 头衔 |
|------|------|------|------|
| < 1200 | 灰 | #808080 | Newbie |
| 1200–1399 | 绿 | #008000 | Pupil |
| 1400–1599 | 青 | #03A89E | Specialist |
| 1600–1899 | 蓝 | #0000FF | Expert |
| 1900–2099 | 紫 | #AA00AA | Candidate Master |
| 2100–2299 | 橙 | #FF8C00 | Master |
| 2300–2399 | 橙 | #FF8C00 | International Master |
| 2400–2599 | 红 | #FF0000 | Grandmaster |
| 2600–2999 | 红 | #FF0000 | International Grandmaster |
| ≥ 3000 | 深红 | #FF0000 | Legendary Grandmaster |

---

## 六、前端设计

### 6.1 技术选型

- **HTML5** + **CSS3**（纯手写，无框架）
- **ECharts 5**（CDN 引入）用于图表渲染
- 暗色主题（`#0f0f1a` 背景 + 玻璃拟态卡片）

### 6.2 页面布局

```
┌─────────────────────────────────────┐
│           用户信息卡片               │
│  [头像] handle (等级分色)           │
│  当前分 │ 最高分 │ 头衔             │
├─────────────────────────────────────┤
│  统计行：总场次 / 近180天 / 最高分  │
├─────────────────────────────────────┤
│        ECharts Rating 折线图         │
│   (节点按等级分色 / 极值标记)       │
├─────────────────────────────────────┤
│        难度直方图 (4时段Tab)         │
├─────────────────────────────────────┤
│         比赛详情表格                  │
│  日期│赛事│排名│分差│各题│补题       │
└─────────────────────────────────────┘
```

### 6.3 响应式设计

| 断点 | 布局 |
|------|------|
| ≥ 1200px | 完整宽屏布局 |
| 768–1199px | 卡片/表格自适应缩放 |
| ≤ 480px | 移动端紧凑布局 |

### 6.4 多用户列表页

- 暗色主题卡片 + 用户摘要表格
- 统计卡片：用户数 / 总场次 / 最高分
- 表头点击排序（数值/字典序）
- 点击用户行跳转个人详情页

---

## 七、编译说明

### 7.1 环境要求

| 组件 | 版本 | 说明 |
|------|------|------|
| MinGW-w64 | 8.1.0+ | GCC 编译器 |
| libcurl | 8.20.0 | HTTP 客户端库（含开发头文件 + 静态库） |
| cJSON | 1.7.18 | JSON 解析（源码已包含） |
| ECharts | 5.x | CDN 引入，无需本地安装 |

### 7.2 编译命令

**方式一：Makefile**
```bash
cd D:\codeforce\Script
mingw32-make          # 编译
mingw32-make test     # 编译并测试（tourist）
mingw32-make dist     # 打包发布
```

**方式二：直接编译**
```bash
gcc -o cf_crawler.exe ^
    main.c http_client.c cf_api.c analyzer.c ^
    data_model.c utils.c cJSON.c ^
    -I. -I"D:/VSCode/VSCplug-in/mingw64/include" ^
    -L"D:/VSCode/VSCplug-in/mingw64/lib" ^
    -lcurl -lws2_32 -O2
```

### 7.3 编译产物

```
Script/
├── cf_crawler.exe         # 可执行程序
├── libcurl-x64.dll        # 运行时依赖（需随 exe 分发）
├── template.html          # 前端页面模板
├── *.h / *.c              # 源代码
└── obj/                   # 中间文件（编译产物）
```

---

## 八、使用说明

### 8.1 单用户
```bash
cf_crawler.exe tourist
# 生成 data.js + index.html，浏览器打开 index.html
```

### 8.2 多用户
```bash
cf_crawler.exe tourist Petr
# 生成 tourist_data.js + tourist.html + Petr_data.js + Petr.html + index.html(列表)
```

### 8.3 文件模式
```
# users.txt（一行一个，支持 # 注释）
tourist
Petr
#KAN
```
```bash
cf_crawler.exe users.txt
```

---

## 九、测试结果

### 测试用例

| # | 命令 | 预期 | 结果 |
|---|------|------|------|
| 1 | `cf_crawler tourist` | 生成 data.js + index.html | ✅ |
| 2 | `cf_crawler tourist Petr` | 各自生成页面 + 列表页 | ✅ |
| 3 | `cf_crawler users.txt` | 读取文件，行为同上 | ✅ |
| 4 | `cf_crawler InvalidUser` | 报错退出，不影响其他 | ✅（HTTP 400 正确处理） |

### 测试数据验证

- `travel` data.js: 55 KB，含 rating 3473, max 4009, "legendary grandmaster"
- `Petr` data.js: 43 KB，含完整比赛历史

---

## 十、目录结构

```
D:\codeforce\
├── 要求.md              # 需求文档
├── 架构.md              # 架构设计
├── 设计文档.md           # 本文档
├── prompt.md            # 开发规则
├── log.md               # 开发日志
├── users.txt            # 多用户列表（示例）
├── Codefoces Rating Clawer-2026.docx  # 原始需求
└── Script/
    ├── main.c           # 程序入口
    ├── http_client.h/c  # HTTP 模块
    ├── cf_api.h/c       # API 调用
    ├── analyzer.h/c     # 数据分析
    ├── data_model.h/c   # 数据结构
    ├── utils.h/c        # 工具函数
    ├── cJSON.h/c        # JSON 库
    ├── template.html    # 前端模板
    ├── Makefile         # 编译脚本
    ├── cf_crawler.exe   # 编译产物
    ├── libcurl-x64.dll  # 运行时依赖
    ├── libcurl/         # curl 开发包（离线备份）
    └── data.js / index.html / ...  # 运行产物
```
